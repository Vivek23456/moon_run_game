# Pump Run

Mobile-friendly tap game (fake numbers — not financial advice).

## Setup

```bash
npm install
npm run dev
```

Open the URL shown in the terminal (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```

Deploy `dist/` to any static host (Vercel, Netlify, Cloudflare Pages).
